# TMUX

## install [tmux](https://github.com/tmux/tmux/wiki/Installing)

``` bash
sudo dnf install -y tmux
```

## copy config files

- `tmux.conf` --> `~/.config/tmux`

- `themes` --> `~/.config/tmux`
